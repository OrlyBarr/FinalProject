# Israel Public Transit - Real-Time Monitoring Platform
# Python Dependencies

# ── Kafka ─────────────────────────────────────────────────
kafka-python-ng>=2.2.3

# ── GTFS-RT Protocol Buffers ──────────────────────────────
gtfs-realtime-bindings==2.0.0    # Google GTFS-RT protobuf bindings
protobuf>=4.24.0

# ── HTTP ──────────────────────────────────────────────────
requests==2.31.0

# ── Data Processing ───────────────────────────────────────
pandas>=2.1.3
pyarrow>=14.0.0
geopy==2.4.1

# ── AWS / Storage ─────────────────────────────────────────
boto3==1.28.0
psycopg2-binary==2.9.9

# NOTE: Apache Airflow runs inside Docker — do not install locally.
# See docker-compose.yml for the Airflow service configuration.

# ── Utilities ─────────────────────────────────────────────
python-dotenv==1.0.0
elasticsearch>=8.0,<9.0

# ── ML — bus delay prediction (ml/train_model.py, ml/predict.py) ──
scikit-learn>=1.3
joblib>=1.3
