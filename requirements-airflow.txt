# requirements-airflow.txt
# All packages needed by Airflow DAGs and tasks.
# Baked into Dockerfile.airflow — do NOT use _PIP_ADDITIONAL_REQUIREMENTS in docker-compose.yml.
# To add a package: add it here, then run: docker compose build

kafka-python==2.0.2
requests==2.31.0
boto3>=1.26,<2.0
psycopg2-binary>=2.9
pandas>=1.5,<2.1
pyarrow>=10.0,<14.0
python-dotenv>=1.0
gtfs-realtime-bindings==2.0.0
geopy>=2.3
elasticsearch>=8.0,<9.0
backports.zoneinfo; python_version < "3.9"
